services:
  - type: web
    name: projeto-x
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /login.jsp
    envVars:
      - key: JAVA_OPTS
        value: -Xmx512m -Xms256m
